[project]
name = "five-dr-v2"
version = "2.0.0"
description = "Deterministic NIFTY 5-day forecasting and efficacy framework"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
