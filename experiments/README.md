# Upstox read-only acquisition experiment

This branch is isolated from canonical 5DR production and must not be merged without explicit approval.

Gates: BUILT -> TESTED -> LIVE VERIFIED -> RELIABILITY VERIFIED -> READY FOR 5DR INTEGRATION.

Current scope is acquisition proof only. The workflow has no schedule, database credentials, production lifecycle writes, forecast release, or trading operations. Upstox observations are not classified as SCREENSHOT or WEB_RESEARCH.
