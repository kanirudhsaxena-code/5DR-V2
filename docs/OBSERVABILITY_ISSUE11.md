# Acquisition observability

Safe operational visibility should expose stage names, request ID, bounded diagnostic code, source authority/status, fallback usage, retrieval timestamps and aggregate blockers.

It must not expose authorization headers, tokens, Cloudflare service credentials, raw provider bodies or unbounded exception strings.
