# Shadow release flags

Every autonomous acquisition envelope currently carries:

- `trading_enabled: false`
- `forecast_release_enabled: false`

These flags are defense-in-depth markers for the shadow acquisition phase. They do not replace the existing Console normalization/execution/publication gates.
