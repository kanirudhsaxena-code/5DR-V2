# Acquisition cost controls

Issue #11 uses existing GitHub Actions and free/read-only data paths first. Network requests are bounded, Upstox retries are capped, and source-registry requests are limited to the small approved registry.

No paid data provider, cloud database, new hosting service or trading API permission is introduced by this branch.
