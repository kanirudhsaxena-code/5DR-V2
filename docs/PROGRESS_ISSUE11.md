# Issue #11 progress checkpoint

The acquisition layer now has concrete code, not only design documentation. The branch includes fail-closed evidence envelopes, Upstox provenance adaptation, controlled web-source fetching, event fallback governance, Market Trust corroboration and execution-input provenance boundaries, plus shadow workflows/tests.

Remaining critical path is cross-repository Console ingestion plus live cross-market/event semantics. Production publication remains disabled.
