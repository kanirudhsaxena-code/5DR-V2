# Acquisition data minimization

The acquisition handoff persists provenance and bounded status metadata, not full third-party provider payloads. Raw responses exist only transiently inside the acquisition process unless a later separately reviewed evidence-retention requirement is approved.

This minimizes secret/data exposure and keeps the Console evidence ledger auditable through source references, timestamps, authority classification, fallback state and SHA-256 response fingerprints.
